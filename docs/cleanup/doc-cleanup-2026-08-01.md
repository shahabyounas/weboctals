# Doc cleanup — 2026-08-01

> Generated by `/shahab-doc-cleanup`. Report-only — nothing has been moved or edited.
> Sections: Summary · Plan inventory · Drift · Product docs · Reorg moves · Orphans · Apply checklist

## Summary
- Total docs scanned: 7
- Plans: 1  (shipped: 1, partial: 0, not started: 0, abandoned: 0)
- Drift findings: 7 (5 in README.md, 2 in DEPLOYMENT-GUIDE.md, plus 1 doc-vs-doc inconsistency)
- Product docs (JSONL): 0 (validation: absent, resurrected markdown: 0)
- Proposed moves: 1
- Orphans: 3 candidates (services.md, TESTING_REPORT.md reference, 2 isolated-but-accurate docs)

## Plan inventory

### docs/superpowers/specs/2026-07-29-organic-redesign-design.md
- **Status:** Shipped (8/8 tasks done per exec.json, overall `status: "complete"`)
- **Created:** 2026-07-29 (would become `docs/plans/2026-07-29-organic-redesign-design.md`)
- **Evidence (from `docs/superpowers/specs/2026-07-29-organic-redesign-design.exec.json`, source of truth per Level 1):**
  - T1–T8 all `"status": "done"`. Notes are detailed and specific (e.g. T1: "scripted sweep replaced 274 hardcoded hex/rgba literals in styles.css"; T8: "npm run build:all exited 0, all entry points compiled").
  - Spot-checked two claims directly against the live codebase rather than trusting the sidecar blindly: `assets/css/styles.css:47-48` does contain `--color-bg: #f5ead8` / `--color-accent: #c67139` (matches T1's claim of Organic tokens shipped), and `--font-heading: 'Caprasimo', serif` is present (matches T1's font claim). Confirmed real.
  - Every task's notes end with some variant of "Verified via parcel build only, no live browser" — the sidecar itself flags this as a known gap, present in every task from T2 onward.
- **Post-ship drift (not in the plan or its sidecar):** substantial additional work happened after the sidecar's `updated_at` (2026-08-01T03:55:00Z) via direct follow-up requests in the same session, none of it tracked by this plan or a new one:
  - Removed the floating "Get in Touch" CTA (`assets/js/main.js`, `assets/css/styles.css`) and the "Start Your Project" form block on `contact.html`.
  - Sitewide phone number changed to `+44 7442 410345` and email consolidated to `info@weboctals.com` (was split across `contact@weboctals.co.uk` / `hello@weboctals.com`) — 19+ files touched.
  - `contact.html` got a JSON-LD structured-data pass (ContactPage/Organization/BreadcrumbList/FAQPage), breadcrumb, and a quotable quick-answer block — none of this exists in the original plan's "Interior pages" section.
  - Favicon and `manifest.json` PWA icons replaced with the new Organic logo mark (previously still the pre-redesign cyan/purple neon mark) — this directly closes part of the "no live browser verification" gap the sidecar flagged, since it was confirmed with an actual headless Chrome session this time.
  - A real responsiveness bug was found and fixed in `.quick-contact-container` / `.cta-buttons`: a CSS cascade-order defect where an unconditional rule appended later in `styles.css` (line ~7582) silently beat an earlier `@media (max-width: 768px)` override (line ~3509) of equal specificity, so the homepage's Quick Contact form never actually stacked on mobile — it stayed squeezed into roughly a third of the viewport. Fixed and confirmed visually via Playwright + real Chrome screenshots at 375–1440px.
  - **Recommended action:** move to `docs/plans/` per the reorg block below. Optionally append a short "Post-ship changes" section to the plan body (or a new dated addendum doc) listing the five bullets above, so a future reader of this plan doesn't assume the shipped state stops at T8. Not urgent — the exec.json's own status is accurate for what it covers, it's just incomplete as a record of the page's current state.

## Drift in reference docs

### README.md
- **Claim (lines 76–80, "Color Palette"):** Primary `#00f5ff` (cyan), Secondary `#7c4dff` (purple), Background `#0a0e27` (dark navy).
- **Reality:** `assets/css/styles.css:47-49` — the site shipped the "Organic" redesign: `--color-bg: #f5ead8` (warm cream), `--color-accent: #c67139` (terracotta), `--color-accent-2: #7a8a5e` (sage). The described palette is the *opposite* of what's live — this isn't drift at the margins, it describes a different design entirely.
- **Claim (lines 83–84, "Typography"):** Primary Font Inter, Code Font JetBrains Mono.
- **Reality:** `--font-heading: 'Caprasimo', serif` / body font is Figtree (confirmed in the exec.json T1 notes and the live stylesheet). Same category of drift as the color palette — this whole "Design System" section (lines 74–92) describes the pre-redesign site.
- **Claim (lines 20–41, "Project Structure"):** tree shows only `index.html`, one blog post, and `assets/images/ # (to be added)`.
- **Reality:** repo root has 17 HTML pages (`about.html`, `services.html`, `contact.html`, `team.html`, 6 service pages, 2 SEO subpages, `machine-learning.html`, plus out-of-scope `home.html`/`services-old.html`/`performance-template.html`/`analytics-template.html`), `blog/` has an index + 4 posts, and `assets/images/` is populated (`logo.svg`, `shahab.png`, `lina.jpeg`, `blog-header-pic.jpg`, `icons/`). The tree also omits `package.json`, `.parcelrc`, `manifest.json`, `sitemap.xml`, `robots.txt`, `sw.js` and every non-index page.
- **Claim (lines 43–65, "Quick Start" → "Local Development"):** instructs `python3 -m http.server 8080` or `npx http-server`.
- **Reality:** `package.json` defines a full Parcel toolchain (`npm run dev`, `npm run build`, `npm run build:all`, etc.) that every other doc (`DEPLOYMENT-GUIDE.md`, `CI-CD-SETUP.md`, the plan's own exec.json) treats as the actual workflow. README never mentions `npm install` or Parcel at all — a new contributor following README literally would get a page with no bundling, no font loading setup, and none of the `<script>`/`<link>` resolution Parcel provides.
- **Claim (line 29 and line 297):** references `TESTING_REPORT.md` twice ("comprehensive testing documentation", "See `TESTING_REPORT.md` for detailed testing documentation").
- **Reality:** `TESTING_REPORT.md` does not exist anywhere in the repo (confirmed via `ls`). Dead reference, twice over.
- **Claim (line 317):** "contact the WebOctals team through the website contact form."
- **Reality:** as of this session, `contact.html` no longer has a form (removed per user request). The only form left on the site is the homepage's Quick Contact form (`index.html`), plus direct `mailto:`/`tel:` links. This line is now actively wrong, not just stale.
- **Recommended edit:** README needs a substantial rewrite, not a patch — the Overview, Design System, Project Structure, and Quick Start sections all describe a materially different site than what's shipped. Given the scope, this is better suited to a fresh `/shahab-plan` pass than a line-by-line fix here.

### DEPLOYMENT-GUIDE.md
- **Claim (line 14):** "CSS files optimized: 91KB (down from 200KB+)."
- **Reality:** `assets/css/styles.css` alone is currently 157,686 bytes (7,825 lines) *unminified* — the Organic redesign added a large component-class layer on top of the original file. The 91KB figure predates that redesign; production-minified size should be re-measured before this claim is trusted again.
- **Claim (line 12):** "20 HTML pages minified (index, contact, services, about, team, etc.)."
- **Reality:** 17 HTML files currently sit at repo root (`ls *.html | wc -l`); with the 5 blog pages that's 22 total build entry points, not the ~25 the doc's phrasing implies ("20... + 5 Blog posts"). Minor drift, low stakes.
- **Not drifted:** Google Analytics ID `G-SM3W8072KB` (line 308) is still live — confirmed present in 4 HTML files.
- **Recommended edit:** re-run `npm run build:all`, note the actual current `dist/` total size and CSS output size, and update lines 12–22 to match. Low priority compared to the README rewrite.

### Doc-vs-doc inconsistency: CI-CD-SETUP.md vs DEPLOYMENT-GUIDE.md
- **CI-CD-SETUP.md** (verified accurate — `.github/workflows/static.yml` matches its description: Node 18, `npm install`, Parcel build, auto-deploy to GitHub Pages on every push to `main`) presents deployment as fully automated.
- **DEPLOYMENT-GUIDE.md** presents deployment as an open manual choice among Netlify (marked RECOMMENDED), Vercel, GitHub Pages, and traditional hosting, with no mention that GitHub Actions already deploys to GitHub Pages automatically on push.
- A reader who only opens `DEPLOYMENT-GUIDE.md` would not know a live auto-deploy pipeline already exists, and might duplicate it manually via Netlify without realizing GitHub Pages is already serving the site on every push.
- **Recommended edit:** add a one-line cross-reference at the top of `DEPLOYMENT-GUIDE.md` pointing at `CI-CD-SETUP.md` and clarifying whether GitHub Pages via Actions is the actual current production target, or whether it's been superseded by one of the other options — I don't have enough context from the repo alone to know which is true today.

## Product docs (JSONL)
No product docs.

## Reorg moves

```bash
git mv docs/superpowers/specs/2026-07-29-organic-redesign-design.md docs/plans/2026-07-29-organic-redesign-design.md
git mv docs/superpowers/specs/2026-07-29-organic-redesign-design.exec.json docs/plans/2026-07-29-organic-redesign-design.exec.json
```

After applying this move, the next `/shahab-plan-executioner` run will rewrite the `plan_file` field inside the sidecar to match the new path — no manual edit needed there.

Note: the exec.json is currently untracked by git (`"tracked": false` in the scan). Confirm you want it committed as part of the move, or add it to `.gitignore` if sidecars are meant to stay local — either is a reasonable convention, but right now it's neither.

## Orphans

- **`services.md`** (764 lines, last touched 2025-09-19, unreferenced by README or any other doc): content describes a "service layer" architecture (`services/core/http.ts`, dependency injection, a section titled "Adding Service Worker to Next.js") that has no corresponding code anywhere in this repo — there is no `services/` directory, no TypeScript, and this is not a Next.js project. This looks like boilerplate from an unrelated project template that landed in this repo by accident. Recommend confirming with a human before deleting (I can't rule out it's an intentional forward-looking design note for a future rebuild), but as it stands it documents nothing that exists. If confirmed orphaned: `git rm services.md`.
- **`TESTING_REPORT.md`** (referenced twice by `README.md`, does not exist): either restore the file or remove the two references — see README drift section above.
- **`ANALYTICS-OPTIMIZATION.md`** and **`CI-CD-SETUP.md`** (273 days untouched each, not linked from README or any other doc): both still appear technically accurate on spot-check (CI-CD-SETUP.md's GitHub Actions description matches the live workflow file). Flagging for visibility only, not because they're wrong — they're just undiscoverable from the README, so a future reader has no way to find them without grepping the repo root.
- No orphan `.exec.json` sidecars found (every sidecar has a matching plan).

## Apply checklist
A suggested order if you decide to act on this report:
1. Review the moves block. Run it once you're happy.
2. Review the drift edits. Apply them by hand or feed them to /shahab-plan / /shahab-plan-executioner. README.md in particular is large enough to warrant its own `/shahab-plan` pass rather than a manual patch.
3. Decide on the orphan files. Delete or archive.
4. After applying, re-run `/understand` so the next audit has a fresh knowledge graph (incremental; or skip if `/understand --auto-update` is on). Note: no knowledge graph existed for this run (`.understand-anything/knowledge-graph.json` not found), so this audit's plan-status checks relied on the exec.json sidecar and direct grep only — a first `/understand` run would make future audits faster and more confident.
5. If you haven't run `/shahab-repo-cleanup` in the last 7 days, consider it now — this audit only covered docs; that one covers deps, dead code, branches, health checks, and the other 10 areas.
